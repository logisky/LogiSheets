// data-gateway craft entry point.
//
// Connects LogiSheets blocks to external systems: import (fetch) structured
// data into a block, or export a block's structured data out. Import/export
// each support one-shot or append-only modes.
//
// Scaffold only — no behaviour implemented yet.

export const NAME = 'data-gateway'
