# what-if-calculator

Scenario analysis over a LogiSheets workbook.

Change input values and see how downstream results shift — the calculator reads
the selection, applies trial values through the engine, and surfaces the
resulting trends, without committing anything to the workbook.

## Build

```bash
yarn workspace what-if-calculator build   # bundles + copies to public/
```
