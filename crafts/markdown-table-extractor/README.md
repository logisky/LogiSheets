# markdown-table-extractor

Parse GitHub-flavored Markdown tables into structured data.

Given Markdown text, it finds the tables and turns them into structured JSON —
the basis for importing them into LogiSheets as **Blocks**. Ships as a UMD
bundle so it can be loaded standalone.

## Build

```bash
yarn workspace markdown-table-extractor build
```
