use crate::{FmtChar, FormattedString, Segment};

pub fn render_number(_num: f64, _seg: Segment) -> FormattedString {
    todo!()
}

pub fn render_placeholders(
    _num: u64,
    _placeholder: &str,
    _before_dot: bool,
    _has_comma: bool,
) -> Vec<FmtChar> {
    todo!()
}
