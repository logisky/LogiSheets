use crate::{FmtChar, FormattedString, Segment};

pub fn render_number(num: f64, seg: Segment) -> FormattedString {
    todo!()
}

pub fn render_placeholders(
    num: u64,
    placeholder: &str,
    before_dot: bool,
    has_comma: bool,
) -> Vec<FmtChar> {
    todo!()
}
